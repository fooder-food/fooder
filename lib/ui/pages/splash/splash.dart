import 'package:flutter/material.dart';

class FooderSplashScreen extends StatelessWidget {
  const FooderSplashScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Splash Screen'),
    );
  }
}
